# Scratch log

Running notes and in-progress thinking for the PathFinder Round 2 project.
Not a source of truth — once a decision is settled, promote it to docs/adr/ and trim the note here.

## 2026-08-22
- Round 2 problem statement, judging criteria, and org's own submission tips confirmed via docs/reference/ screenshots and the live portal.
- Portal bug: "Marked as submitted" shows green even with all 5 deliverable fields empty — verify actual field contents before the deadline, don't trust the flag.
- Full planning pass complete: stack, LLM provider, recommendation engine, service boundary, auth, and the multi-agent workflow are all recorded in docs/adr/0001-0006.
- git initialized locally. Not yet connected to a GitHub remote.

## 2026-08-23
- GitHub repo confirmed: https://github.com/SumedhaKar/HCLTech (had a default "Initial commit" with just README.md — rebased scaffold onto it, not force-pushed).
- `gh` CLI installed (winget) and authenticated as SumedhaKar (device-code flow).
- PR #1 (scaffold) and PR #3 (catalog seed data, rebased after #1 merged since its original base branch was deleted) both merged into main. Local main synced, stale branches cleaned up.
- Supabase setup explicitly deferred by the user ("let's do it afterwards") — migration/seed files exist but nothing's been applied anywhere yet.

## 2026-08-23 (cont.)
- Slices 1 (FE catalog) and 2 (BE chat intake) both completed and PR'd (#5, #4). The process crashed mid-run and restarted; both agents had already committed and pushed before that happened, so no work was lost, but the recovery surfaced a real operational gap: this session dispatches subagents into the *same* working directory rather than isolated worktrees, and two agents doing `git checkout -b`/commit concurrently nearly collided (FE's uncommitted work got rescued via a targeted `git stash push --keep-index -- frontend/` mid-race). Worth using `isolation: "worktree"` for any future parallel agent dispatch instead.
- Somewhere in the crash/restart, PR #4 and #5 got merged without INT ever completing a verification pass — the responsible process is unclear (possibly a pre-crash INT agent still running, possibly leftover from an earlier turn). Not reverted since the code was sound, but it's exactly the gap INT was supposed to close, and it bit us: see next line.
- User added a real `GEMINI_API_KEY` to `backend/.env`. Live-tested `/chat/intake` for the first time and found two real bugs the mocked unit tests couldn't catch: (1) `response_schema` as a `types.Schema` instance crashes this SDK version — must be a plain dict; (2) `gemini-2.0-flash-lite` is retired, API pointed at `gemini-3.5-flash-lite`. Fixed both, verified live call + tests both pass, PR #6 opened.
- Separately: `gh pr merge` is blocked by Claude Code's own auto-mode safety classifier (harness-level, not a CLAUDE.md setting) — PRs can be opened but not merged from here. #6 needs a manual merge.
- Self-inflicted near-miss: ran `git checkout main && git reset --hard origin/main` while my own uncommitted fix was sitting in the working tree, wiping it. Recovered by reapplying the same two-line fix from memory since I'd just written and verified it, but the lesson is to check `git status`/stash before any `reset --hard`, full stop.

## 2026-08-23 (cont. 2)
- PR #6 merged (manually, after the harness classifier blocked `gh pr merge`).
- Turns out Slice 3 (recommendation engine + profile/path CRUD) had *already* been dispatched before the crash, sitting uncommitted in two isolated git worktrees (`.claude/worktrees/agent-*`, branches `feat/be-recommendations` and `feat/fe-profile-path`) — correction to the note above: at least this pair of agents used real worktree isolation, not the shared directory. Whatever caused the Slice-1/2 near-miss is still worth watching for, but it isn't a blanket "this session never isolates agents" problem.
- Finished both from where they stopped: BE's recommendation engine (rule-based filter → Gemini-embedding ranking) + path generator + `/chat/explain`, FE's learner-profile and learning-path CRUD routes. Both carried the same live-API bugs as PR #6 (schema-must-be-a-dict; retired model names) since they were written before that fix existed — same fix pattern applied, plus a third stale model found (`text-embedding-004` → `gemini-embedding-001`, confirmed via a live `ListModels` REST call rather than guessed). Also added a missing `greenlet` dependency (SQLAlchemy async needs it; surfaced as 5 failing tests until added).
- Both rebased cleanly onto post-#6 main, tests pass (24/24 BE), builds pass (FE), live Gemini calls (chat intake, embeddings) verified working. PRs opened: **#7** (BE recommendations) and **#8** (FE profile/path). Both still need a manual merge (harness classifier).
- Worktrees removed (`git worktree remove` for one, files were locked for the other — force-removed then `git worktree prune`), `.claude/worktrees/` added to `.gitignore` so this doesn't recur.

## Open, not yet resolved
- Supabase: account/project creation deferred, revisit when the user is ready. Blocks: applying the schema migration, running the catalog seed, and any real end-to-end verification (recommendation generation, learning-path CRUD) against live data.
- Vercel/Render aren't yet linked to this specific GitHub repo — dashboard action on the user's side, not yet started.
- PRs #7 and #8 need a manual merge — blocked by the harness classifier, not by review.
- Recurring pattern worth naming: every Gemini-calling piece of code written *before* a live key existed has had stale model names and/or the schema-as-dict issue. If more AI-calling code gets written blind (no live key), assume it needs the same pass once it can finally be tested for real.
